/* Foreign-language scorecard data for polyglot.html.
   Scores are 0 to 5 per axis: how well a language fits the C-ABI `load_foreign` slot that Zig
   and Swift use, plus the route a language would need instead. Edit here to re-score. */
const TIERS = {
  supported: { label: "Supported today", color: "var(--t-supported)", order: 0 },
  ready:     { label: "Ready to add",    color: "var(--t-ready)",     order: 1 },
  caveat:    { label: "Fits with caveats", color: "var(--t-caveat)", order: 2 },
  path:      { label: "Needs a different path", color: "var(--t-path)", order: 3 },
  feature:   { label: "Would want a new feature", color: "var(--t-feature)", order: 4 },
  no:        { label: "Not viable",      color: "var(--t-no)",        order: 5 },
};
const AXES = ["export","build","runtime","tooling","types","check","cross","payoff"];
const AXIS_LABEL = { export:"Export", build:"Build", runtime:"Runtime", tooling:"Tooling", types:"Types", check:"Check", cross:"Cross", payoff:"Payoff" };

// s = [export, build, runtime, tooling, types, check, cross, payoff]
const LANGS = [
  {
    name: "Zig", tier: "supported", s: [5,5,5,5,5,5,5,4],
    one: "The reference point. One binary, no runtime, exact C symbols.",
    why: {
      export: "`export fn` puts a function under its exact C name. Nothing else needed.",
      build: "`zig build-obj` emits one object; the driver packs it with `ar` or `libtool`.",
      runtime: "Zig has no runtime to link. The object is self-contained.",
      tooling: "A single downloadable binary that also bundles a C compiler.",
      types: "`i64`, `f64`, `bool`, `void` map one to one.",
      check: "`zig build-obj -fno-emit-bin` type-checks without writing output.",
      cross: "Zig cross-compiles to almost any target with a `-target` flag. Not wired yet, but trivial.",
      payoff: "Fast, tiny, and increasingly popular for systems and game code.",
    },
    pros: ["Already works.", "Easiest possible mental model: write a function, mark it export, done.", "Cross-compiling later would be almost free."],
    cons: ["Small standard library compared to mainstream languages.", "The language is still pre-1.0 and changes between releases."],
    work: "Nothing. This row exists so you can see what a perfect fit looks like.",
  },
  {
    name: "Swift", tier: "supported", s: [4,4,2,3,5,5,2,4],
    one: "The reference point for a language that drags a runtime along.",
    why: {
      export: "`@_cdecl(\"name\")` or `@c` on 6.3+. Works, but is an attribute people forget.",
      build: "`swiftc -emit-library -static -parse-as-library` gives a `.a` in one command.",
      runtime: "`swiftCore` must be found through `swiftc -print-target-info` and linked. That is the whole reason `orch_link_swift_runtime` exists.",
      tooling: "Needs Xcode or the swift.org toolchain. Large install, slow first build.",
      types: "`Int64`, `Double`, `Bool` map exactly.",
      check: "`swiftc -typecheck` is a proper check-only mode.",
      cross: "Swift cross-compiling outside Apple platforms is still awkward.",
      payoff: "Access to Apple frameworks and a big developer base.",
    },
    pros: ["Already works.", "Readable, safe, modern language.", "Natural choice if the user is on a Mac."],
    cons: ["Heavy toolchain.", "Linking the runtime is fiddly and platform-specific.", "Linux support exists but is second-class."],
    work: "Nothing. This row shows the ceiling for a runtime-carrying language.",
  },
  {
    name: "C", tier: "supported", s: [5,5,5,5,5,5,4,4],
    one: "The ABI everyone else is imitating.",
    why: {
      export: "Every non-static function is already a C symbol.",
      build: "Compiled by `cc-rs` inside the generated `build.rs`.",
      runtime: "libc is already linked by Rust.",
      tooling: "A C compiler is required by Rust itself, so it is always present.",
      types: "`int64_t`, `double`, `bool`, `void`.",
      check: "`cc -fsyntax-only`.",
      cross: "`cc-rs` handles cross targets if a cross compiler exists.",
      payoff: "Decades of libraries and the fastest way to wrap anything.",
    },
    pros: ["Already works.", "Zero surprises.", "Every other language on this page can talk to it."],
    cons: ["Easy to write memory bugs.", "No help from the compiler beyond the basics."],
    work: "Nothing.",
  },
  {
    name: "C++", tier: "supported", s: [5,5,4,5,5,5,4,5],
    one: "C with a bigger standard library and an `extern \"C\"` escape hatch.",
    why: {
      export: "`extern \"C\"` turns off name mangling for the exported function.",
      build: "`cc-rs` with `.cpp(true)`.",
      runtime: "`libstdc++` or `libc++` gets linked automatically by `cc-rs`. Slight weight.",
      tooling: "Ships with every C toolchain.",
      types: "Same as C.",
      check: "`c++ -fsyntax-only`.",
      cross: "Same story as C.",
      payoff: "Largest pool of high-performance libraries anywhere.",
    },
    pros: ["Already works.", "Huge ecosystem: physics, math, media, graphics."],
    cons: ["Complex language.", "Templates and exceptions must never cross the boundary."],
    work: "Nothing.",
  },
  {
    name: "Rust", tier: "supported", s: [5,5,5,5,5,3,5,5],
    one: "Supported by a different route: the file is pasted into the generated crate.",
    why: {
      export: "Not an FFI. The `.rs` file is injected verbatim, so functions are called directly.",
      build: "Cargo builds it with everything else.",
      runtime: "None beyond what OrchestrateLang already ships.",
      tooling: "Cargo is required anyway.",
      types: "The sidecar can even use `string` here, because there is no C boundary.",
      check: "Deferred to the deep pass, because the file may call generated module code.",
      cross: "Cargo `--target` works normally.",
      payoff: "Safe, fast, and the host language of the whole system.",
    },
    pros: ["Already works, and supports strings.", "No unsafe boundary at all."],
    cons: ["Foreign Rust must be synchronous, no `.await`.", "Compile times."],
    work: "Nothing.",
  },
  {
    name: "TypeScript", tier: "supported", s: [1,1,1,3,4,5,3,5],
    one: "Supported through a Bun executable bridge, not the C ABI.",
    why: {
      export: "No C symbols. Calls go over a persistent process protocol.",
      build: "`bun build --compile` or scriptc produces an executable per module.",
      runtime: "A whole JavaScript runtime per module.",
      tooling: "Needs Bun or scriptc plus TypeScript 7.",
      types: "Supports `int`, `float`, `bool`, `string`, arrays, and structs via JSON-like marshaling.",
      check: "TypeScript's own checker runs first.",
      cross: "Bun can target other platforms when compiling.",
      payoff: "Largest developer population on earth.",
    },
    pros: ["Already works, with richer types than the C-ABI path.", "Module state survives between calls."],
    cons: ["Every call crosses a process boundary, so it is slower than a native call.", "Adds a runtime dependency to the built binary."],
    work: "Nothing. Listed so the bridge route is visible next to the native one.",
  },

  {
    name: "D", tier: "ready", s: [5,5,3,4,5,5,3,3],
    one: "Closest sibling to Zig among mature languages, with an optional no-runtime mode.",
    why: {
      export: "`extern(C)` on a function exports its plain C name.",
      build: "`dmd -lib` or `ldc2 -lib` emits a `.a` from one file.",
      runtime: "Normal D links `druntime` and `phobos`. Compile with `-betterC` and there is no runtime at all, exactly like Zig.",
      tooling: "`dmd` or `ldc2` is a single install through most package managers.",
      types: "`long`, `double`, `bool`, `void` match exactly.",
      check: "`dmd -o-` parses and type-checks without emitting anything.",
      cross: "LDC supports `-mtriple`, but you need the target's libraries on hand.",
      payoff: "Fast compiles, solid standard library, but a small community.",
    },
    pros: ["Looks like C++ without the sharp edges.", "Compiles very fast.", "Can be told to behave exactly like Zig for FFI purposes."],
    cons: ["Few people write D today, so fewer examples and libraries.", "Two runtime modes means two sets of gotchas."],
    work: "Add a `ForeignSource::D` variant, a `foreign_toolchain` entry for `dmd`/`ldc2`, one `build.rs` line, and a `checker` entry. Decide whether to default to `-betterC`.",
  },
  {
    name: "Nim", tier: "ready", s: [5,4,4,4,5,5,4,3],
    one: "Python-looking syntax that compiles through C into a static library.",
    why: {
      export: "`{.exportc, cdecl.}` on a proc gives it the exact C symbol.",
      build: "`nim c --app:staticlib --noMain` emits a `.a`. It goes through the C compiler you already have.",
      runtime: "The memory manager is compiled into the archive. Pure numeric functions need no `NimMain()` call; anything allocating does.",
      tooling: "`nim` plus a C compiler. Small install.",
      types: "`int64`, `float64`, `bool` are exact.",
      check: "`nim check` is a dedicated check-only command.",
      cross: "Because it emits C, cross-compiling is as easy as cross-compiling C.",
      payoff: "Pleasant, productive, and fast. Modest community.",
    },
    pros: ["Reads like Python, runs like C.", "Static archive with no extra runtime library to hunt for.", "Cross-compiling later would be simple."],
    cons: ["The garbage collector must be initialised if functions allocate, which the sidecar cannot see.", "Compiles through C, so error messages sometimes point at generated C."],
    work: "Same four edits as Zig. Document that sidecar functions must be allocation-free or call `NimMain()` themselves.",
  },
  {
    name: "Odin", tier: "ready", s: [5,4,5,4,5,5,3,2],
    one: "Zig's closest cousin in spirit: small, explicit, no runtime.",
    why: {
      export: "`@(export)` with `\"c\"` calling convention exports a plain C symbol.",
      build: "`odin build file.odin -file -build-mode:obj` emits an object to archive, like Zig.",
      runtime: "Effectively none in object mode.",
      tooling: "Single download, but binaries are platform-specific and Windows-first in polish.",
      types: "`i64`, `f64`, `bool` are exact.",
      check: "`odin check` exists.",
      cross: "Has `-target`, but far less battle-tested than Zig.",
      payoff: "Beloved in game-dev circles, tiny everywhere else.",
    },
    pros: ["Almost identical shape to Zig from the driver's point of view.", "Very easy to read."],
    cons: ["Small ecosystem.", "Fewer users means fewer people to ask when something breaks."],
    work: "Same four edits as Zig. Object plus archiver, like the Zig path.",
  },
  {
    name: "Fortran", tier: "ready", s: [5,5,3,4,5,5,3,4],
    one: "The numeric workhorse, and modern Fortran binds to C by name.",
    why: {
      export: "`bind(C, name=\"...\")` pins the exact C symbol. No mangling.",
      build: "`gfortran -c` then the archiver, exactly like the Zig path.",
      runtime: "`libgfortran` is needed for I/O and some intrinsics. Pure math often works without it, but linking it is the safe default.",
      tooling: "`gfortran` is one package on every platform; `flang` is a newer option.",
      types: "`integer(c_int64_t)`, `real(c_double)`, `logical(c_bool)` from `iso_c_binding` match exactly.",
      check: "`gfortran -fsyntax-only`.",
      cross: "Works where a cross `gfortran` exists, which is uncommon.",
      payoff: "Unmatched for numerical kernels, and huge existing scientific codebases.",
    },
    pros: ["Blazing fast for math and arrays.", "Enormous library of proven scientific code.", "The current int/float/bool sidecar is exactly what Fortran is good at."],
    cons: ["Old-fashioned syntax.", "Few new developers learn it.", "Arrays across the boundary would need the planned richer sidecar types."],
    work: "Same four edits as Zig, plus one link line for `libgfortran` modelled on the Swift runtime helper.",
  },

  {
    name: "Go", tier: "caveat", s: [5,4,1,4,4,5,4,5],
    one: "Exports cleanly, but only one Go archive may live in a process.",
    why: {
      export: "`//export name` above a function.",
      build: "`go build -buildmode=c-archive` produces a `.a` and a header.",
      runtime: "The Go runtime starts its own threads and installs signal handlers. Only one Go archive can be linked per process, which breaks the one-file-per-load pattern.",
      tooling: "Single install, fast builds.",
      types: "`int64`, `float64`, `bool` are fine. Strings and slices are not.",
      check: "`go vet` or `go build -o /dev/null`.",
      cross: "Go cross-compiles well, but cgo needs a C cross compiler too.",
      payoff: "Massive ecosystem for networking, cloud APIs, and tooling.",
    },
    pros: ["Simple language, great libraries.", "Exporting a function is one comment line."],
    cons: ["All Go sources across every module would have to be merged into one archive.", "The Go runtime lives in your process forever, with its own scheduler and memory.", "Roughly a megabyte of extra binary size."],
    work: "A merged build step: collect every Go file into one package, build one archive, link once. Different enough that it is its own feature, not a copy of Zig's.",
    feature: "A project-wide `load_foreign` that merges many source files into one library. Useful for Go, and later for any language whose compiler wants a whole package at once.",
  },
  {
    name: "Ada", tier: "caveat", s: [5,4,3,3,5,5,3,3],
    one: "Safety-critical language with a clean C export, but needs a binder step.",
    why: {
      export: "`pragma Export(C, Fn, \"name\")` sets the exact symbol.",
      build: "`gnatmake -c` compiles, but `gnatbind -n` must run for a library with no main, then the archiver.",
      runtime: "`libgnat` is normally linked. A zero-footprint runtime profile exists for pure code.",
      tooling: "GNAT ships with GCC on Linux; elsewhere you install via Alire.",
      types: "`Interfaces.C.long` is 64-bit on Unix but 32-bit on Windows. `Interfaces.Integer_64` is the safe choice.",
      check: "`gcc -c -gnatc` is a proper semantic check.",
      cross: "GNAT cross toolchains exist for embedded targets, less so for desktop pairs.",
      payoff: "Strong contracts and range types, popular in aerospace and rail.",
    },
    pros: ["The compiler catches whole classes of mistakes.", "Exact C exports."],
    cons: ["Verbose.", "The extra bind step means a fifth edit, not four.", "Small modern community."],
    work: "Zig's four edits plus a `gnatbind -n` call and a runtime link helper.",
  },
  {
    name: "Free Pascal", tier: "caveat", s: [5,3,3,4,5,3,4,2],
    one: "Exports fine, but there is no tidy single-file static library command.",
    why: {
      export: "`cdecl; export;` plus an `exports` clause in a `library`.",
      build: "`fpc` emits `.o` files per unit but expects to link a program or a shared library. Archiving the objects works but is manual.",
      runtime: "The Pascal RTL must be linked and initialised for anything beyond arithmetic.",
      tooling: "`fpc` is a single package on all platforms.",
      types: "`Int64`, `Double`, `Boolean` are exact.",
      check: "No true syntax-only flag. `-Cn` skips linking, which is close enough.",
      cross: "Free Pascal cross-compiles unusually well.",
      payoff: "Niche today, though Lazarus and Delphi refugees exist.",
    },
    pros: ["Fast compiler.", "Very good cross-compilation story."],
    cons: ["Awkward library-mode build.", "Aging community and fewer modern libraries."],
    work: "Object-plus-archiver path like Zig, with extra handling of RTL objects. Expect a few surprises.",
  },
  {
    name: "Objective-C", tier: "caveat", s: [5,5,3,3,5,5,2,3],
    one: "A C superset, so the C path almost works, but only comfortably on macOS.",
    why: {
      export: "Plain C functions are exported as usual.",
      build: "`cc-rs` compiles `.m` files with clang.",
      runtime: "`libobjc` and usually Foundation must be linked.",
      tooling: "Trivial on macOS with Xcode. GNUstep on Linux is a rough experience.",
      types: "Same as C.",
      check: "`clang -fsyntax-only`.",
      cross: "Effectively Apple-only in practice.",
      payoff: "Direct access to Apple system APIs from a C-shaped language.",
    },
    pros: ["Almost no new build machinery.", "Talk to Apple frameworks without Swift's runtime dance."],
    cons: ["Practically macOS-only.", "Being replaced by Swift everywhere."],
    work: "Route `.m` files through the C path and add a framework-link line. Small, but only worth it if macOS users ask.",
  },
  {
    name: "Assembly", tier: "caveat", s: [5,5,5,4,3,4,2,2],
    one: "Perfect ABI fit, but every platform needs its own source file.",
    why: {
      export: "A `global` label is a C symbol.",
      build: "`nasm -f elf64` or `as`, then the archiver.",
      runtime: "None whatsoever.",
      tooling: "`as` ships with every C toolchain; `nasm` is one package.",
      types: "You place values in registers yourself, and the calling convention differs on Windows.",
      check: "Assembling is the check.",
      cross: "Each architecture and OS needs separate source, so cross-compiling is really rewriting.",
      payoff: "Hand-tuned kernels only. Almost nobody needs this.",
    },
    pros: ["Absolute control and speed.", "No runtime, no surprises from a compiler."],
    cons: ["Not portable at all.", "Very easy to get the calling convention subtly wrong."],
    work: "Object plus archiver, like Zig, with per-platform file selection. Cheap to add, rarely used.",
  },
  {
    name: "Vala", tier: "caveat", s: [4,4,3,3,5,4,4,2],
    one: "Compiles to C, but everything is tied to GLib.",
    why: {
      export: "Functions become C functions, but with a namespace prefix unless you set `[CCode (cname = \"...\")]`.",
      build: "`valac -C` emits C, then the C path compiles it.",
      runtime: "GLib and GObject are required and must be linked.",
      tooling: "`valac` plus GLib development headers, which are a pain outside Linux.",
      types: "`int64`, `double`, `bool` are exact.",
      check: "`valac -C` without compiling is effectively a check.",
      cross: "As easy as C, once GLib for the target exists.",
      payoff: "Only relevant to GNOME desktop developers.",
    },
    pros: ["C# style syntax that becomes plain C.", "Cross-compiles like C."],
    cons: ["Needs GLib installed everywhere.", "Tiny community outside GNOME."],
    work: "Two-stage build through the C path plus a GLib link line. Doable, low demand.",
  },
  {
    name: "C# (Native AOT)", tier: "supported", s: [5,4,2,2,4,2,3,4],
    one: "Shipped in 0.10.0. About 5 ns a call, against 1 to 2 ns for the same function in C.",
    why: {
      export: "`[UnmanagedCallersOnly(EntryPoint = \"name\")]` sets the exact symbol.",
      build: "`dotnet publish -p:NativeLib=Static` works, but wants a `.csproj`, so a single-file model needs a generated project.",
      runtime: "A multi-megabyte static runtime with a garbage collector, plus several system libraries to link.",
      tooling: "The .NET SDK is hundreds of megabytes and a publish takes many seconds.",
      types: "`long`, `double` are exact. `bool` is not blittable in exports, so use `byte` and convert.",
      check: "Not covered by `check-foreign`: `dotnet` has no syntax-only check for one file outside a project.",
      cross: "Native AOT builds for the host only; `build --lib --target` elsewhere fails with a clear message.",
      payoff: "Huge ecosystem, especially for business and Windows code.",
    },
    pros: ["Enormous library ecosystem.", "Modern, safe language many teams already use."],
    cons: ["Heavy install and slow builds.", "Big binaries.", "Booleans need a workaround."],
    work: "Nothing. Each module publishes its own shared library rather than a static archive, because two Native AOT static archives cannot go into one program: each embeds its own runtime. A C# module brings .NET's garbage collector into the process.",
  },
  {
    name: "Crystal", tier: "caveat", s: [4,3,2,3,5,4,3,2],
    one: "Ruby-flavoured and compiled, but its garbage collector expects to own `main`.",
    why: {
      export: "A top-level `fun name(...)` defines a C-callable function.",
      build: "`crystal build --cross-compile` emits an object, but linking needs `libgc`, `libpcre2`, and `libevent`.",
      runtime: "Boehm GC must be initialised through Crystal's own startup. Allocating functions without it crash.",
      tooling: "One package, but Windows support is young.",
      types: "`Int64`, `Float64`, `Bool` are exact.",
      check: "`crystal build --no-codegen`.",
      cross: "The cross-compile flag exists but you must link on the target.",
      payoff: "Small but enthusiastic community.",
    },
    pros: ["Pleasant Ruby-like syntax.", "Genuinely fast output."],
    cons: ["Runtime initialisation is a trap for anything beyond arithmetic.", "Three extra libraries to link."],
    work: "Object plus archiver plus a runtime link helper, with a documented allocation-free rule.",
  },
  {
    name: "V", tier: "caveat", s: [3,3,3,4,4,4,3,1],
    one: "Simple language, but the toolchain is too young to lean on.",
    why: {
      export: "`@[export: 'name']` exists but has had bugs across releases.",
      build: "Compiles through C. `-shared` is documented; static output needs `-o file.o` and manual archiving.",
      runtime: "Small, but `-no-main` and initialisation behaviour keep changing.",
      tooling: "One binary plus a C compiler.",
      types: "`i64`, `f64`, `bool` map well.",
      check: "`v -check`.",
      cross: "Through C, so plausible.",
      payoff: "Immature ecosystem and a history of over-promising.",
    },
    pros: ["Easy to read.", "Fast compiles."],
    cons: ["Breaking changes between versions.", "Few libraries."],
    work: "Possible via the C path, but every V release could break it. Wait.",
  },
  {
    name: "Hare", tier: "caveat", s: [5,4,4,3,4,3,2,1],
    one: "A Zig-like minimalist language, but Linux and BSD only.",
    why: {
      export: "`export fn` with `@symbol(\"name\")` gives the exact C symbol.",
      build: "`hare build -t o` emits an object to archive.",
      runtime: "A small runtime object is linked, close to none.",
      tooling: "Needs the QBE backend and harec. No macOS or Windows.",
      types: "`i64`, `f64`, `bool` are exact.",
      check: "No dedicated check mode; a build to an object is the check.",
      cross: "Cross-compiles among Linux and BSD targets only.",
      payoff: "Tiny community.",
    },
    pros: ["Same shape as Zig from the driver's side.", "Very simple language."],
    cons: ["No macOS, no Windows.", "Almost nobody uses it."],
    work: "Zig's four edits, but gated to Linux and BSD hosts.",
  },

  {
    name: "Lua", tier: "path", s: [1,3,3,4,4,4,4,3],
    one: "Not a C-ABI language, but its interpreter is so small it could be embedded through a generated C shim.",
    why: {
      export: "Lua functions have no C symbols. A generated C file would create a `lua_State` and expose each sidecar function.",
      build: "`liblua` is a handful of C files and links statically through the existing C path.",
      runtime: "A `lua_State` is a few hundred kilobytes and starts instantly.",
      tooling: "`lua` and `luac` are one small package.",
      types: "Lua numbers are `int64` or `double` by default. Booleans are native.",
      check: "`luac -p` parses without running.",
      cross: "Lua is plain ANSI C, so it goes wherever C goes.",
      payoff: "Scripting-in-the-loop for game logic and config, familiar to many.",
    },
    pros: ["Tiny and fast to start.", "The whole interpreter is smaller than Swift's runtime."],
    cons: ["Every call goes through an interpreter, so slower than native.", "Requires generating C glue, which is a new kind of feature."],
    work: "Generate a C shim per Lua file that loads the script and wraps each sidecar function. Reuses the C path, but it is a different design from Zig.",
    feature: "The embedded-interpreter mode described under Python, with Lua as the cheapest first implementation because the whole interpreter is a few C files.",
  },
  {
    name: "Haskell", tier: "path", s: [4,3,1,2,4,5,1,3],
    one: "Can export C functions, but the runtime must be started first.",
    why: {
      export: "`foreign export ccall` works.",
      build: "`ghc -c` compiles, then you link against GHC's RTS libraries.",
      runtime: "`hs_init()` must run before any call, and the RTS brings a garbage collector and threads.",
      tooling: "GHC via ghcup is a large install.",
      types: "`Int64`, `Double`, `CBool` map well.",
      check: "`ghc -fno-code` type-checks only.",
      cross: "GHC cross-compiling is notoriously hard.",
      payoff: "Powerful type system, niche in practice.",
    },
    pros: ["Extremely expressive.", "Type checker catches a lot."],
    cons: ["Runtime startup and RTS linking are complicated.", "Huge toolchain."],
    work: "Runtime init hook plus RTS linking. Better served by a landline serverlet.",
  },
  {
    name: "OCaml", tier: "path", s: [2,3,1,3,3,4,2,3],
    one: "Fast native code, but no direct C export and 63-bit integers.",
    why: {
      export: "Requires a hand-written C stub that calls `caml_callback`.",
      build: "`ocamlopt -output-obj` produces an object.",
      runtime: "`caml_startup` must run first and the GC owns memory.",
      tooling: "opam is fine on Unix, awkward on Windows.",
      types: "Native `int` is 63-bit and floats are boxed. `Int64` is a separate boxed type.",
      check: "`ocamlopt -stop-after typing`.",
      cross: "Limited.",
      payoff: "Excellent for compilers and finance, small community.",
    },
    pros: ["Fast and safe.", "Great for language tooling."],
    cons: ["Integers are not 64-bit by default.", "You must write C glue anyway."],
    work: "Generated C stubs and runtime init. A landline serverlet is the sane route.",
  },
  {
    name: "Java (GraalVM)", tier: "path", s: [4,3,1,1,4,3,2,4],
    one: "Native Image can export C entry points, but each call needs an isolate.",
    why: {
      export: "`@CEntryPoint(name = \"...\")` exists.",
      build: "`native-image --shared` produces a library, with a Maven or Gradle project around it.",
      runtime: "An isolate must be created and attached per thread, and binaries are tens of megabytes.",
      tooling: "GraalVM plus a build tool. Very heavy and slow.",
      types: "Primitives map, but every entry point takes an isolate argument first.",
      check: "`javac` compiles, no lighter mode.",
      cross: "Not supported by Native Image in general.",
      payoff: "Enormous ecosystem.",
    },
    pros: ["Access to the whole Java world.", "Well-documented export mechanism."],
    cons: ["Massive toolchain.", "Extra isolate argument breaks the sidecar's simple signature.", "Minutes-long builds."],
    work: "Would need sidecar signatures to grow a hidden isolate parameter. Use a landline serverlet.",
  },
  {
    name: "Kotlin/Native", tier: "path", s: [4,3,2,1,4,3,3,3],
    one: "Produces a static library, but through Gradle and with its own GC.",
    why: {
      export: "`@CName(\"name\")` exports a C symbol and a header.",
      build: "`kotlinc-native -produce static` exists, but real projects use Gradle.",
      runtime: "Kotlin/Native runtime with a garbage collector is linked in.",
      tooling: "Needs a JVM to run the compiler and Gradle to be practical. Slow.",
      types: "`Long`, `Double`, `Boolean` map.",
      check: "No fast check mode; compiling is the check.",
      cross: "Has real targets, including iOS.",
      payoff: "Popular for Android and shared mobile code.",
    },
    pros: ["Modern language with a big following.", "Exports and cross-targets are real features."],
    cons: ["Slow, JVM-hosted compiler.", "Gradle is the opposite of one-file simplicity."],
    work: "Possible but painful. The build step alone would dwarf every other language here.",
  },
  {
    name: "Scala Native", tier: "path", s: [4,3,2,1,4,3,2,2],
    one: "Same story as Kotlin/Native with a smaller audience.",
    why: {
      export: "`@exported(\"name\")` on a function.",
      build: "Through sbt only in practice.",
      runtime: "Garbage collector and runtime linked in.",
      tooling: "sbt plus a JVM. Slow.",
      types: "Primitives map.",
      check: "Compile is the check.",
      cross: "Limited.",
      payoff: "Small.",
    },
    pros: ["Expressive language."],
    cons: ["Everything about the build.", "Few users."],
    work: "Not worth it. Use a landline serverlet.",
  },
  {
    name: "Dart", tier: "path", s: [3,1,1,3,4,5,2,3],
    one: "Compiles to native, but not to a library that exposes C functions.",
    why: {
      export: "Dart can be called from C only through the Dart VM's embedding API.",
      build: "`dart compile` produces executables or AOT snapshots, not static libraries with your symbols.",
      runtime: "The Dart runtime must be embedded and started.",
      tooling: "Simple SDK install.",
      types: "Primitives map.",
      check: "`dart analyze` is excellent.",
      cross: "Limited.",
      payoff: "Flutter developers.",
    },
    pros: ["Nice language, good analyzer."],
    cons: ["No path to a plain static library."],
    work: "Landline serverlet only.",
  },
  {
    name: "Julia", tier: "path", s: [2,2,1,2,5,2,1,4],
    one: "Wonderful for numerics, but it is a JIT that wants to own the process.",
    why: {
      export: "`@ccallable` exists, but only inside a PackageCompiler-built library.",
      build: "`PackageCompiler.create_library` takes minutes and produces hundreds of megabytes.",
      runtime: "`jl_init` must run, then the JIT compiles on first call.",
      tooling: "Large install, slow startup.",
      types: "`Int64`, `Float64`, `Bool` are exact.",
      check: "No real check-only mode.",
      cross: "Not supported.",
      payoff: "Top-tier scientific computing.",
    },
    pros: ["Fast math with readable code.", "Great scientific libraries."],
    cons: ["Startup and build times are enormous.", "The runtime is heavy and stateful."],
    work: "Landline serverlet. Do not try to make this a static library.",
  },
  {
    name: "Python", tier: "path", s: [1,1,1,4,2,5,1,5],
    one: "The roadmap already says it: Python needs its interpreter, so it belongs in a landline serverlet.",
    why: {
      export: "No C symbols.",
      build: "Nothing to build.",
      runtime: "The full CPython interpreter.",
      tooling: "Installed almost everywhere.",
      types: "Everything is a dynamic object; conversion is explicit.",
      check: "`py_compile` already runs in the `check` command.",
      cross: "Not applicable.",
      payoff: "Largest library ecosystem for data and AI.",
    },
    pros: ["Everyone knows it.", "Libraries for everything."],
    cons: ["Slow and interpreted.", "Cannot be linked as a static function library."],
    work: "Already handled by design: landline serverlets. Nothing to add on the FFI side.",
    feature: "An embedded-interpreter mode: link CPython into the process through `pyo3` so calls are in-process function calls instead of messages. Faster than a landline, still stateful, and the same idea would cover Lua and Ruby.",
  },
  {
    name: "Ruby", tier: "path", s: [1,1,1,4,2,4,1,3],
    one: "Same situation as Python with a smaller upside.",
    why: {
      export: "No C symbols.",
      build: "Nothing to build.",
      runtime: "Full interpreter.",
      tooling: "Common.",
      types: "Dynamic.",
      check: "`ruby -c`.",
      cross: "Not applicable.",
      payoff: "Web and scripting.",
    },
    pros: ["Expressive."],
    cons: ["Interpreted, cannot be statically linked as functions."],
    work: "Landline serverlet only.",
  },
  {
    name: "Mojo", tier: "path", s: [3,3,3,2,4,3,1,3],
    one: "Promising and C-friendly on paper, but the toolchain changes too fast to build on today.",
    why: {
      export: "An `@export` mechanism exists and the C ABI story is improving, but the details have shifted between releases.",
      build: "Can emit shared libraries; static library support is not a stable, documented path yet.",
      runtime: "Lighter than Python, but not as free-standing as Zig.",
      tooling: "Installed through Modular's own package manager, Linux and macOS only.",
      types: "`Int64`, `Float64`, `Bool` map.",
      check: "A build is the check.",
      cross: "Not there yet.",
      payoff: "Could be very attractive for AI kernels if it settles.",
    },
    pros: ["Python-like syntax with systems-level speed.", "Strong momentum."],
    cons: ["Pre-1.0 and changing.", "Vendor-controlled toolchain."],
    work: "Re-evaluate in a year. Scores here reflect a moving target.",
  },
  {
    name: "Common Lisp", tier: "no", s: [2,2,1,2,3,3,1,2],
    one: "Image-based runtimes do not fit a static library model.",
    why: {
      export: "ECL can produce C-callable code; SBCL needs alien callbacks and a saved core.",
      build: "No single-file static library path.",
      runtime: "A whole Lisp image must be loaded.",
      tooling: "Varies by implementation.",
      types: "Mapping exists but is implementation-specific.",
      check: "Load the file in the REPL.",
      cross: "Rare.",
      payoff: "Niche.",
    },
    pros: ["Unique interactive development."],
    cons: ["Wrong shape for this feature entirely."],
    work: "Not viable through FFI. Landline if anyone ever asks.",
  },
  {
    name: "Elixir / Erlang", tier: "no", s: [1,1,1,3,2,4,1,3],
    one: "Runs on the BEAM virtual machine, which cannot be linked as a function library.",
    why: {
      export: "No C symbols.",
      build: "Compiles to BEAM bytecode.",
      runtime: "The BEAM VM.",
      tooling: "Fine, but irrelevant here.",
      types: "Dynamic.",
      check: "`elixirc` can compile without running.",
      cross: "Not applicable.",
      payoff: "Excellent for concurrent servers, which is a landline use case, not an FFI one.",
    },
    pros: ["Superb at concurrency."],
    cons: ["Entire VM, no static linking."],
    work: "Not viable through FFI. A landline serverlet would actually suit it well.",
  },
  {
    name: "JavaScript", tier: "ready", s: [1,1,1,3,4,4,3,5],
    one: "The TypeScript bridge already runs JavaScript. A plain `.js` file is one extension check away.",
    why: {
      export: "No C symbols. Calls go over the same persistent process protocol TypeScript uses.",
      build: "Bun compiles `.js` exactly as it compiles `.ts`.",
      runtime: "A JavaScript runtime per module, same as TypeScript.",
      tooling: "Bun or scriptc, already required for TypeScript.",
      types: "Same marshaling as TypeScript: numbers, booleans, strings, arrays, structs.",
      check: "Bun can parse-check, but there is no type checker without TypeScript.",
      cross: "Bun can target other platforms when compiling.",
      payoff: "The most widely known language there is.",
    },
    pros: ["Practically free to add.", "Everyone can write it."],
    cons: ["No types means the sidecar is the only contract.", "Slower than a native call, like TypeScript."],
    work: "Accept `.js` in the TypeScript bridge, skip the type-check step, and document it. A small change.",
  },
  {
    name: "PHP", tier: "path", s: [1,1,1,3,2,4,1,4],
    one: "Interpreted and web-shaped. Belongs in a landline serverlet, not the FFI.",
    why: {
      export: "No C symbols.",
      build: "Nothing to build.",
      runtime: "The full PHP interpreter.",
      tooling: "Common on servers, less so on developer machines.",
      types: "Dynamic, with loose number and boolean conversion.",
      check: "`php -l` lints a file without running it.",
      cross: "Not applicable.",
      payoff: "Huge existing web codebases: WordPress, Laravel, Symfony.",
    },
    pros: ["Enormous installed base.", "Easy to find developers."],
    cons: ["Interpreted, no static linking.", "Its process model assumes a web request, not a long-lived actor."],
    work: "A PHP landline serverlet speaking the same protocol the TypeScript bridge uses. No FFI route.",
  },
  {
    name: "Perl", tier: "path", s: [1,1,1,4,2,4,1,2],
    one: "Installed everywhere, but the same interpreter story as Python and Ruby.",
    why: {
      export: "No C symbols.",
      build: "Nothing to build.",
      runtime: "Full interpreter.",
      tooling: "Ships with almost every Unix.",
      types: "Dynamic scalars.",
      check: "`perl -c` compiles without running.",
      cross: "Not applicable.",
      payoff: "Text processing and legacy systems.",
    },
    pros: ["Already on the machine.", "Unbeatable for regex-heavy scripts."],
    cons: ["Declining community.", "No static linking route."],
    work: "Landline serverlet only.",
  },
  {
    name: "R", tier: "path", s: [1,2,1,3,3,3,1,4],
    one: "The statistics language. Its value is data frames, which the sidecar cannot express yet.",
    why: {
      export: "R functions have no C symbols. R can embed via `libR`, but it wants to own the process.",
      build: "Packages compile C and Fortran, but the R code itself is interpreted.",
      runtime: "The R interpreter, single-threaded and global.",
      tooling: "One install, large.",
      types: "Everything is a vector. A scalar `int` or `float` is a length-one vector.",
      check: "`R -e 'parse(file=...)'` checks syntax.",
      cross: "Not applicable.",
      payoff: "Best statistics and plotting libraries anywhere.",
    },
    pros: ["Thousands of statistical packages.", "Analysts already know it."],
    cons: ["Slow, single-threaded interpreter.", "Passing single numbers back and forth misses the point of R."],
    work: "Landline serverlet, like Python.",
    feature: "A table or data-frame value type in the sidecar and the language, so a foreign call can return rows rather than one number. R, SQL and Python's pandas would all use it.",
  },
  {
    name: "JVM languages", tier: "path", s: [1,2,1,2,4,4,1,5],
    one: "Java, Kotlin and Scala on the ordinary JVM. Huge ecosystem, but the JVM is a process, not a library.",
    why: {
      export: "No C symbols without JNI or GraalVM, and JNI runs the other direction.",
      build: "Produces a jar, which only a JVM can run.",
      runtime: "A whole JVM with its own garbage collector and thread pool.",
      tooling: "Maven or Gradle plus a JDK. Slow and heavy.",
      types: "Primitives map, but they still live behind the JVM.",
      check: "`javac` and `kotlinc` compile without running.",
      cross: "Not applicable; the JVM is the portability layer.",
      payoff: "Spring, Android, Kafka, the whole enterprise world.",
    },
    pros: ["Largest business ecosystem.", "One route covers three languages."],
    cons: ["The JVM must be started and kept alive.", "Startup takes seconds."],
    work: "A JVM landline: a small Java host that loads a jar and speaks the bridge protocol. One feature, three languages.",
    feature: "A generic bridge-protocol host so any runtime can become a landline by implementing one small server. TypeScript already speaks it; JVM, .NET and Python would follow.",
  },
  {
    name: "Shell commands", tier: "feature", s: [1,1,2,5,2,4,1,3],
    one: "Not a language to compile, but the thing people most often want to call.",
    why: {
      export: "No symbols. A command is invoked, not linked.",
      build: "Nothing to build.",
      runtime: "A process per call.",
      tooling: "Always present.",
      types: "Arguments and stdout are strings; numbers must be parsed.",
      check: "`bash -n` checks syntax.",
      cross: "Scripts are platform-specific.",
      payoff: "Wrapping existing CLI tools is the most common integration request in any system.",
    },
    pros: ["Wrap any existing tool in minutes.", "No compiler required."],
    cons: ["Slow: a process per call.", "Everything is text.", "Windows is a different world."],
    work: "Not an FFI. It needs its own small feature.",
    feature: "A `load_foreign \"sh\"` where each sidecar function names a command, arguments become argv, and stdout is parsed into the return type. Cheap to build and immediately useful.",
  },
  {
    name: "WebAssembly", tier: "supported", s: [4,5,4,4,4,4,5,5],
    one: "Shipped in 0.9.0. Not one language but a target: the loader admits every language that compiles to it.",
    why: {
      export: "Wasm exports are named functions. Rust, C, Zig, Go, AssemblyScript and Grain all produce them.",
      build: "The module is the artifact. The driver already builds sandbox guests for `wasm32-wasip1`.",
      runtime: "Wasmtime is a Rust crate, so it links like any dependency.",
      tooling: "Each source language needs its own compiler, but the loader needs nothing extra.",
      types: "Wasm `i64` and `f64` are exact; `bool` becomes `i32`.",
      check: "Validation is built into every wasm runtime.",
      cross: "Wasm is the same bytes on every platform. Cross-compiling stops being a problem.",
      payoff: "Sandboxing, portability, and a dozen languages for the price of one feature.",
    },
    pros: ["Untrusted code cannot touch the host.", "Same module runs on every OS.", "Solves the cross-compile gap for Zig and Swift too."],
    cons: ["Calls cross a runtime boundary, slower than a native call.", "Strings and arrays need memory-copy glue."],
    work: "Nothing. `load_foreign \"wasm\"` reads the module's export table and checks every sidecar signature against it; imports are denied by default. Strings need the module to export `orch_alloc` and `orch_free`.",
  },
  {
    name: "GPU kernels", tier: "feature", s: [3,4,2,2,3,4,1,5],
    one: "CUDA, OpenCL and WGSL. They only make sense once arrays can cross the boundary.",
    why: {
      export: "`nvcc` compiles host-side `extern \"C\"` wrappers that launch kernels. OpenCL and WGSL need a host program.",
      build: "`nvcc -c` gives an object to archive, like Zig.",
      runtime: "`cudart` or an OpenCL driver must be linked and a device must exist.",
      tooling: "Vendor SDKs, large and picky about driver versions.",
      types: "Scalars are fine, but a kernel that takes a single number is pointless.",
      check: "`nvcc --dryrun` or a compile with no link.",
      cross: "Tied to the GPU vendor.",
      payoff: "Orders-of-magnitude speedups for the right workload.",
    },
    pros: ["Massive parallel speed for math, imaging, and AI.", "The CUDA object path looks just like Zig's."],
    cons: ["Needs a GPU and a vendor SDK.", "Useless without array arguments."],
    work: "Object-plus-archiver like Zig, plus vendor runtime linking. Only worth it after the sidecar can pass arrays.",
    feature: "Arrays and buffer handles in the sidecar. Every C-ABI language on this page wants that too; GPU kernels are the row that makes it urgent.",
  },
  {
    name: "SQL (SQLite, DuckDB)", tier: "feature", s: [1,4,4,4,3,3,4,5],
    one: "Not a function language, but SQLite is a single C file and DuckDB a single C++ file.",
    why: {
      export: "Queries have no symbols. A generated C shim would prepare each sidecar function as a statement.",
      build: "The SQLite amalgamation compiles through the existing C path.",
      runtime: "SQLite is small and embedded. DuckDB is larger but still in-process.",
      tooling: "Nothing beyond a C compiler.",
      types: "Scalars are fine. Results are rows.",
      check: "`sqlite3` can `EXPLAIN` a statement without running it.",
      cross: "Plain C, so it goes anywhere.",
      payoff: "Half of all real-world scripting is querying data.",
    },
    pros: ["No extra install.", "Persistent local data with one line of setup."],
    cons: ["Only scalar results fit today's sidecar.", "Needs generated glue like Lua would."],
    work: "A `load_foreign \"sql\"` where each sidecar function is a prepared statement. Small once row-shaped return types exist.",
    feature: "The same table value type R wants. SQL is the row where it would be used most.",
  },
  {
    name: "C3", tier: "ready", s: [5,5,5,4,5,4,4,2],
    one: "A C evolution with a Zig-shaped compiler: one binary, static libs, no runtime.",
    why: { export: "Functions are C-ABI by default; `@export` pins the name.", build: "`c3c static-lib` or `compile-only` emits an object to archive.", runtime: "None.", tooling: "Single download, LLVM inside.", types: "`long`, `double`, `bool` exact.", check: "`c3c compile-only` without linking is the check; no dedicated flag.", cross: "`--target` exists and works for common triples.", payoff: "Young, small community." },
    pros: ["Reads like C with the sharp edges filed off.", "Same driver shape as Zig."], cons: ["Pre-1.0.", "Few users."],
    work: "Zig's four edits.",
  },
  {
    name: "Beef", tier: "caveat", s: [5,3,3,2,5,3,2,2],
    one: "C#-flavoured systems language from the game world, but Windows-first.",
    why: { export: "`[Export, CLink]` gives a C symbol.", build: "Emits static libs through the IDE or `BeefBuild`; project-file driven.", runtime: "Small runtime object, must be linked.", tooling: "Windows primary; Linux and macOS builds are rough.", types: "`int64`, `double`, `bool` exact.", check: "No check-only mode.", cross: "Limited.", payoff: "Niche game-dev following." },
    pros: ["Fast compiles, nice syntax."], cons: ["Project files, not single files.", "Windows-centric."],
    work: "Generate a Beef project per file. Only worth it for Windows game users.",
  },
  {
    name: "F# (Native AOT)", tier: "caveat", s: [4,4,2,2,4,4,3,3],
    one: "Same route as C# Native AOT, with a few F#-specific AOT gaps.",
    why: { export: "`[<UnmanagedCallersOnly(EntryPoint=...)>]` works on static members.", build: "`dotnet publish -p:NativeLib=Static`.", runtime: "The .NET runtime, multi-megabyte.", tooling: ".NET SDK, heavy and slow.", types: "`int64`, `float`, but `bool` must be `byte`.", check: "`dotnet build`.", cross: "Limited pairs.", payoff: "Functional .NET, smaller crowd than C#." },
    pros: ["Elegant functional code on .NET."], cons: ["All of C#'s toolchain weight.", "Some F# features trim badly under AOT."],
    work: "Ride the C# path, which shipped in 0.10.0. Some F# features still trim badly under Native AOT, so it is not a copy of the C# row.",
  },
  {
    name: "Carbon", tier: "no", s: [3,1,3,1,4,1,1,1],
    one: "Google's C++ successor is still an experiment with no usable toolchain.",
    why: { export: "C interop is the design goal.", build: "No production compiler.", runtime: "Planned to be none.", tooling: "Build from source, experimental.", types: "Would map.", check: "None.", cross: "None.", payoff: "Nothing to use yet." },
    pros: ["Could become important."], cons: ["Not shipping."],
    work: "Revisit in a few years.",
  },
  {
    name: "Jai", tier: "no", s: [4,3,4,1,4,2,2,1],
    one: "Closed beta. Cannot be depended on by a public project.",
    why: { export: "`#c_call` and `#program_export` exist.", build: "Can emit objects.", runtime: "Small.", tooling: "Invite-only compiler.", types: "`s64`, `float64`, `bool` exact.", check: "Compile is the check.", cross: "Some.", payoff: "Cannot ask users to install it." },
    pros: ["Fast and pragmatic by reputation."], cons: ["Not publicly available."],
    work: "Nothing until it is public.",
  },
  {
    name: "Chicken Scheme", tier: "caveat", s: [4,4,2,3,4,4,4,2],
    one: "A Scheme that compiles to C and can export C functions, but its GC must be started.",
    why: { export: "`define-external` creates a C-callable function.", build: "`csc -c` emits an object via C.", runtime: "`libchicken` must be linked and `CHICKEN_initialize` called.", tooling: "One package.", types: "Integers and flonums map; booleans map.", check: "`csc -analyze-only`.", cross: "Through C.", payoff: "Small." },
    pros: ["Real C exports from a Lisp."], cons: ["Runtime initialisation trap, like Crystal."],
    work: "Object path plus a runtime init hook.",
  },
  {
    name: "Racket", tier: "path", s: [1,2,1,3,3,4,1,2],
    one: "Chez-based Scheme that embeds, but only as a whole runtime.",
    why: { export: "No C exports; C calls Racket through `racket_boot`.", build: "`raco exe` builds executables, not libraries.", runtime: "Whole Racket VM.", tooling: "Large install.", types: "Map through the FFI layer.", check: "`raco expand`.", cross: "No.", payoff: "Teaching and language research." },
    pros: ["Superb for building little languages."], cons: ["Wrong shape for a static function."],
    work: "Landline serverlet.",
  },
  {
    name: "Clojure", tier: "path", s: [1,2,1,2,3,3,1,4],
    one: "JVM Lisp with real industry use. Same route as the other JVM languages.",
    why: { export: "No C symbols.", build: "Jars only.", runtime: "The JVM.", tooling: "Leiningen or the CLI plus a JDK.", types: "Boxed by default.", check: "Compile with `clojure -M`.", cross: "Not applicable.", payoff: "Data-processing shops love it." },
    pros: ["Immutable data, great for pipelines."], cons: ["Slow JVM startup."],
    work: "The JVM landline host.",
  },
  {
    name: "Gleam", tier: "path", s: [1,1,1,4,4,5,1,3],
    one: "Typed language for the BEAM, or JavaScript. Two routes, neither native.",
    why: { export: "No C symbols.", build: "Emits Erlang or JavaScript.", runtime: "BEAM or a JS runtime.", tooling: "Single binary, pleasant.", types: "Static and clean.", check: "`gleam check`.", cross: "Not applicable.", payoff: "Growing fast." },
    pros: ["Friendly, well-typed."], cons: ["Always needs a VM."],
    work: "JS target through the TypeScript bridge would be the cheapest route.",
  },
  {
    name: "Cython", tier: "path", s: [3,4,1,3,4,4,2,4],
    one: "Compiles to C, but the C it makes needs CPython.",
    why: { export: "`cdef public api` functions get C symbols.", build: "`cython` then the C path.", runtime: "Requires an initialised CPython interpreter.", tooling: "pip install, needs Python headers.", types: "`long long`, `double`, `bint` exact.", check: "`cython` without compiling.", cross: "Only with a target Python.", payoff: "Speeds up existing Python code." },
    pros: ["Familiar to scientific Python users."], cons: ["Still Python underneath."],
    work: "Only meaningful with the embedded-interpreter feature.",
    feature: "The embedded CPython mode. Cython would then be the fast lane inside it.",
  },
  {
    name: "Pony", tier: "caveat", s: [4,3,2,3,4,3,2,1],
    one: "Actor language with a real static-library mode, but its scheduler wants the process.",
    why: { export: "`@name` bare functions are exportable in library mode.", build: "`ponyc --library` emits a `.a` and header.", runtime: "The actor scheduler must be started with `pony_init`.", tooling: "One package.", types: "`I64`, `F64`, `Bool` exact.", check: "`ponyc --pass=expr`.", cross: "Weak.", payoff: "Tiny community." },
    pros: ["Capabilities-secure concurrency."], cons: ["Runtime init and threads."],
    work: "Library path plus init hook. Low demand.",
  },
  {
    name: "AssemblyScript", tier: "supported", s: [4,5,5,4,4,4,5,3],
    one: "TypeScript syntax that compiles straight to WebAssembly, which the wasm loader already takes.",
    why: { export: "`export function` becomes a wasm export.", build: "`asc` emits a `.wasm` file.", runtime: "Minimal, inside the module.", tooling: "npm install.", types: "`i64`, `f64`, `bool` exact.", check: "`asc --noEmit`.", cross: "Wasm is portable.", payoff: "Web developers writing native-speed code." },
    pros: ["Familiar syntax, tiny output."], cons: ["Only useful once a wasm loader exists."],
    work: "Nothing. Compile to `.wasm` with `asc` and load it with `load_foreign \"wasm\"`.",
  },
  {
    name: "Grain", tier: "supported", s: [4,5,4,3,4,4,5,1],
    one: "Wasm-first functional language, so the wasm loader already takes it.",
    why: { export: "`provide` exports become wasm exports.", build: "`grain compile` emits `.wasm`.", runtime: "Inside the module.", tooling: "One binary.", types: "Numbers map, some boxed.", check: "Compile is the check.", cross: "Wasm.", payoff: "Very small." },
    pros: ["Clean design."], cons: ["Few users."],
    work: "Nothing. `grain compile` emits a `.wasm` the loader reads directly.",
  },
  {
    name: "MoonBit", tier: "caveat", s: [4,4,4,3,5,4,4,2],
    one: "New language with wasm and a native C backend.",
    why: { export: "`pub` functions with `#export` reach C.", build: "`moon build --target native` emits C then objects.", runtime: "Small, embedded in output.", tooling: "One installer, still changing.", types: "`Int64`, `Double`, `Bool` exact.", check: "`moon check`.", cross: "Wasm or through C.", payoff: "Rising, unproven." },
    pros: ["Fast builds, two backends."], cons: ["Young and moving."],
    work: "Either backend works now: the C path, or `moon build --target wasm` through the wasm loader.",
  },
  {
    name: "Koka", tier: "caveat", s: [4,4,4,3,4,4,4,1],
    one: "Effect-typed research language that compiles to clean C.",
    why: { export: "`extern` C exports exist.", build: "`koka -c` emits C to compile with the C path.", runtime: "Small reference-counting runtime linked as C.", tooling: "One binary.", types: "`int64`, `float64`, `bool` map.", check: "Type-check without codegen.", cross: "Through C.", payoff: "Research." },
    pros: ["Fast output, no GC."], cons: ["Academic community."],
    work: "Two-stage C path.",
  },
  {
    name: "Futhark", tier: "feature", s: [5,5,3,3,3,5,2,4],
    one: "A GPU array language that emits a C library with a plain C API.",
    why: { export: "`entry` functions become C functions.", build: "`futhark cuda --library` or `multicore --library` emits `.c` and `.h`.", runtime: "Needs CUDA or OpenCL for GPU backends; the multicore backend needs only pthreads.", tooling: "One binary.", types: "Scalars fine, but every real entry takes arrays.", check: "`futhark check`.", cross: "Vendor-bound on GPU.", payoff: "Serious speed for numeric code without writing CUDA." },
    pros: ["Write array math, get a GPU kernel.", "Real C API, unlike raw CUDA."], cons: ["Pointless without array arguments."],
    work: "C path plus backend runtime linking.",
    feature: "Arrays in the sidecar. Futhark is the nicest way to justify them.",
  },
  {
    name: "Chapel", tier: "caveat", s: [4,3,2,2,4,4,2,2],
    one: "HPC language with a library mode, dragging a parallel runtime.",
    why: { export: "`export proc` gives a C symbol.", build: "`chpl --library` emits a `.a` and header.", runtime: "Chapel runtime must be initialised with `chpl_library_init`.", tooling: "Build from source, large.", types: "`int(64)`, `real(64)`, `bool` exact.", check: "`chpl --parse-only`.", cross: "Cluster targets only.", payoff: "Supercomputer users." },
    pros: ["Parallel loops for free."], cons: ["Heavy runtime, heavy install."],
    work: "Library path plus init hook.",
  },
  {
    name: "COBOL (GnuCOBOL)", tier: "caveat", s: [4,4,3,3,3,4,3,3],
    one: "Legacy giant. GnuCOBOL compiles to C with callable entry points.",
    why: { export: "Each program is a C function of its `PROGRAM-ID`; arguments via `USING`.", build: "`cobc -c` emits objects via C.", runtime: "`libcob` must be linked and initialised.", tooling: "One package on Linux, harder elsewhere.", types: "COBOL numerics are decimal pictures, not `i64`; needs conversion.", check: "`cobc -fsyntax-only`.", cross: "Through C.", payoff: "Banks still run it." },
    pros: ["Reuse existing business logic."], cons: ["Type model does not match.", "Fixed-format source."],
    work: "Object path plus `libcob` linking and numeric marshaling.",
  },
  {
    name: "Prolog (SWI)", tier: "path", s: [1,2,2,3,2,3,2,2],
    one: "Logic programming. Embeds through libswipl, as a whole engine.",
    why: { export: "No exports; C queries the engine.", build: "Saved states, not libraries.", runtime: "`libswipl`, moderate size.", tooling: "One package.", types: "Terms, not scalars.", check: "`swipl -c`.", cross: "Weak.", payoff: "Rules engines." },
    pros: ["Unique for rules and search."], cons: ["Query model, not function model."],
    work: "Landline serverlet, or an embedded-engine feature.",
  },
  {
    name: "Elm", tier: "path", s: [1,1,1,4,4,5,3,2],
    one: "Compiles only to JavaScript, so it could ride the TypeScript bridge.",
    why: { export: "Ports, not functions.", build: "`elm make` emits JS.", runtime: "A JS runtime.", tooling: "One binary.", types: "Strong.", check: "`elm make --output=/dev/null`.", cross: "Wherever the bridge runs.", payoff: "Front-end only." },
    pros: ["No runtime exceptions."], cons: ["Built for browsers, not function calls."],
    work: "Through the JavaScript bridge with a port adapter.",
  },
  {
    name: "ReScript", tier: "path", s: [1,1,1,4,4,5,3,2],
    one: "OCaml-flavoured language that emits readable JavaScript.",
    why: { export: "JS exports.", build: "`rescript build` emits JS.", runtime: "A JS runtime.", tooling: "npm.", types: "Strong.", check: "Build is the check.", cross: "Bridge.", payoff: "Small." },
    pros: ["Types on top of JS."], cons: ["Just JS at the end."],
    work: "Through the JavaScript bridge.",
  },
  {
    name: "Haxe", tier: "caveat", s: [3,3,2,3,4,4,3,3],
    one: "Multi-target language. Its C++ target could link, its JS target could bridge.",
    why: { export: "`@:cppExport` on hxcpp.", build: "hxcpp emits C++ and a static lib.", runtime: "hxcpp GC runtime.", tooling: "Haxe plus hxcpp plus a C++ compiler.", types: "`Int` is 32-bit; `haxe.Int64` needed.", check: "`haxe --no-output`.", cross: "Decent.", payoff: "Game and cross-platform shops." },
    pros: ["One source, many targets."], cons: ["32-bit Int default.", "GC runtime."],
    work: "C++ path plus runtime, or the JS bridge.",
  },
  {
    name: "MATLAB / Octave", tier: "path", s: [2,2,1,2,3,2,1,4],
    one: "Numeric giants. MATLAB Coder can emit C; Octave embeds as a whole.",
    why: { export: "MATLAB Coder generates C with entry functions; Octave has none.", build: "Coder output compiles through the C path.", runtime: "Coder output is standalone; Octave needs its interpreter.", tooling: "MATLAB is proprietary and expensive.", types: "Everything is a matrix.", check: "Weak.", cross: "Coder can target embedded.", payoff: "Engineering departments." },
    pros: ["Existing engineering code."], cons: ["Licensing.", "Matrix-shaped."],
    work: "Coder output through the C path; Octave as a landline.",
    feature: "Arrays in the sidecar.",
  },
  {
    name: "Tcl", tier: "path", s: [1,4,4,4,3,4,4,2],
    one: "Designed to be embedded. Small interpreter, like Lua.",
    why: { export: "None; a C shim would call `Tcl_Eval`.", build: "`libtcl` links statically.", runtime: "Small.", tooling: "One package.", types: "Everything is a string.", check: "`tclsh` with `info complete`.", cross: "Plain C.", payoff: "Legacy tooling and EDA." },
    pros: ["Trivial to embed."], cons: ["String-typed."],
    work: "Same shim as Lua.",
    feature: "The embedded-interpreter mode.",
  },
  {
    name: "Wren", tier: "path", s: [1,4,5,4,4,3,4,1],
    one: "Tiny embeddable scripting language from the Lua school.",
    why: { export: "None; C calls into the VM.", build: "A handful of C files.", runtime: "Under 100 KB.", tooling: "Vendor the source.", types: "Numbers are doubles; `int` is lossy above 2^53.", check: "Parse via the VM.", cross: "Plain C.", payoff: "Game scripting niche." },
    pros: ["Smallest runtime here."], cons: ["No 64-bit integers."],
    work: "Same shim as Lua.",
    feature: "The embedded-interpreter mode.",
  },
  {
    name: "Janet", tier: "path", s: [1,5,4,4,4,4,4,1],
    one: "A Lisp that ships as one C file, made to be embedded.",
    why: { export: "None; C calls `janet_dostring`.", build: "Single-file amalgamation via the C path.", runtime: "Small.", tooling: "Vendor the file.", types: "Numbers are doubles; `int64` via boxed abstract type.", check: "Parse via the VM.", cross: "Plain C.", payoff: "Small." },
    pros: ["Easiest possible embed."], cons: ["Tiny community."],
    work: "Same shim as Lua.",
    feature: "The embedded-interpreter mode.",
  },
  {
    name: "FreeBASIC", tier: "caveat", s: [5,4,3,4,5,4,3,1],
    one: "Modern BASIC compiling through GCC with C exports.",
    why: { export: "`Extern \"C\"` blocks plus `Export`.", build: "`fbc -lib` emits a `.a`.", runtime: "Small FB runtime linked.", tooling: "One package.", types: "`LongInt`, `Double`, `Boolean` exact.", check: "No check-only; `-c` compiles only.", cross: "Some.", payoff: "Hobbyist." },
    pros: ["Simple.", "Real static libs."], cons: ["Nobody asks for it."],
    work: "Zig's four edits plus runtime link.",
  },
  {
    name: "Standard ML (MLton)", tier: "caveat", s: [4,3,2,3,4,4,2,1],
    one: "Whole-program ML compiler with `_export` to C.",
    why: { export: "`_export \"name\": ...` creates a C symbol.", build: "`mlton -format library` emits a `.a`.", runtime: "MLton runtime linked, needs an open call.", tooling: "One package, slow builds.", types: "`Int64`, `Real64`, `bool` exact.", check: "`mlton -stop tc`.", cross: "Weak.", payoff: "Academic." },
    pros: ["Very fast output."], cons: ["Slow whole-program compiles.", "Runtime init."],
    work: "Library path plus init hook.",
  },
  {
    name: "Terra", tier: "caveat", s: [5,5,5,2,5,3,3,1],
    one: "Lua-hosted low-level language that saves real object files with a C ABI.",
    why: { export: "`terralib.saveobj` exports named functions.", build: "Emits `.o` or `.a` directly.", runtime: "None in the output.", tooling: "Needs LLVM and LuaJIT; awkward installs.", types: "`int64`, `double`, `bool` exact.", check: "Run the Lua script.", cross: "LLVM targets.", payoff: "Research." },
    pros: ["Perfect ABI fit."], cons: ["Install pain.", "Tiny community."],
    work: "Object path, like Zig.",
  },
  {
    name: "PowerShell", tier: "path", s: [1,1,1,4,2,4,2,4],
    one: "The Windows shell. Same shape as shell commands, with objects instead of text.",
    why: { export: "None.", build: "Nothing.", runtime: ".NET runtime.", tooling: "Present on Windows, installable elsewhere.", types: "Objects, converted on output.", check: "`Parser::ParseFile`.", cross: "Runs on Linux too.", payoff: "Windows automation." },
    pros: ["Wrap any Windows admin task."], cons: ["Slow startup, process per call."],
    work: "The shell-command feature with a PowerShell host.",
    feature: "The `load_foreign \"sh\"` command feature, extended to PowerShell on Windows.",
  },
];

window.ORCH_SCORECARD = { TIERS: TIERS, AXES: AXES, AXIS_LABEL: AXIS_LABEL, LANGS: LANGS };
